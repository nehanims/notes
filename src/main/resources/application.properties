spring.application.name=voice
# Default profile is development
spring.profiles.active=dev
# TODO most of these configurations should be moved to dev and prod profiles
logging.level.root=INFO

# Server Configuration
#logging.level.org.springframework.kafka=DEBUG
#logging.level.org.apache.kafka=DEBUG

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-http-form-post-size=20MB



# H2 Database Configuration
spring.datasource.url=jdbc:h2:file:./data/voice-notes-db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console Configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Minio Configuration
minio.url=http://localhost:9000
# TODO don't use this key in production
minio.access-key=zWB6yTTqJqhQlw7QZO4i
minio.secret-key=8Ws6iy38h8x0QncY0B5ZXXqPOlPekdZ29bdQvn0g
minio.bucket-name=voice-notes

# Kafka Configuration
kafka.topics.voice-note=voice-note
kafka.topics.voice-note-transcription=voice-note-transcription
kafka.bootstrap-address=localhost:9092

# Transcription Endpoint Configuration
transcription.endpoint.host=http://localhost:9900
# Allowed origin for CORS
cors.allowed-origin=http://localhost:3000


